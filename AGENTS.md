# Project Instructions

- Keep the public plugin on documented OpenCode APIs.
- Do not override the built-in `bash` tool.
- Treat native TUI plugin APIs as experimental until OpenCode documents them.
- Run `npm test` before considering a change complete.
